# LOOTSHOT 开发清单

## v4.1：按住连射与后期成长重平衡

方案：[v4.1-hold-fire-balance-plan.md](v4.1-hold-fire-balance-plan.md)

状态：已完成并通过自动测试、WebGL 构建与浏览器验收。

- [x] 先写持续射击、逐段强化门槛和基准经济路线测试，确认旧实现不能通过。
- [x] 为武器加入递增射速、每级弹量和共用的 16 级强化价格。
- [x] 写入新版 8 枪 24 关生命、奖励、新枪价格与金币强化价格。
- [x] 实现左键按下立即射击、按住持续射击、松开/取消/失焦停止。
- [x] 更新主菜单射速、总强化、价格和弹量预览，重建场景与中文字形。
- [x] 用全新浏览器站点存储验证从零流程，并运行 EditMode、PlayMode、WebGL 与浏览器验收。

## v4：8 枪 24 关数值测试原型

方案：[v4-8x24-test-prototype-plan.md](v4-8x24-test-prototype-plan.md)

状态：已完成首轮测试原型；时长偏差记录在平衡报告中。

- [x] 记录现有工作树并在隔离副本完成 v3 基线测试：19/19。
- [x] 先写 v4 数据、解锁边界和完全重置测试，确认 RED 后实现。
- [x] 扩展为 8 枪、24 关、12 组普通/强化 Boss，并切换独立 v4 测试存档。
- [x] 增加 8 枪栏、8×3 手动选关面板和模块化占位轮廓。
- [x] 完成三条成长路线模拟并写入平衡报告。
- [x] 运行完整 EditMode、生成 Gzip WebGL，并完成浏览器验收。

## v3：WebGL 第一版原型（已完成）

日期：2026-09-14  
方案：[plan.md](plan.md)  
状态：已完成。v2 记录保存在 [archive/v2](archive/v2/)。

## T1：手动射击与关卡推进

- [x] 先写 EditMode 用例：一击一弹、弹尽等待、失败保留金币且不推进、胜利才推进、必须返回主菜单后才能再次开战。
- [x] 将自动计时战斗改为 `Menu → Fighting/Resolving → Won/Lost → Menu`。
- [x] 删除 Session 与界面的再战入口，战斗中禁止所有购买和选择。

## T2：独立武器强化与 v3 存档

- [x] 每把枪分别保存火力和弹量等级；金币倍率每级线性增加 3%。
- [x] 保存当前武器、最高已购武器和当前 Boss 选择。
- [x] 使用独立 v3 文件/PlayerPrefs 存档，保留 v1/v2 文件。

## T3：4 枪 12 Boss 数值切片

- [x] 写入 4 把枪、12 个 Boss 和升级价格固定表。
- [x] 验证武器价格递增、每组三关约 50%/75%/110% 弹量门槛。
- [x] 真实 Session 模拟无金币死锁，失败后至有效强化不超过两轮。

## T4：独立主菜单

- [x] 实现角色区、武器卡、火力/弹量/金币升级、Boss 选择、金币、进度、战斗按钮和重置。
- [x] 只允许在主菜单购买、换枪和选关。
- [x] 默认保留失败目标；首通后默认切换下一关。

## T5：独立战斗界面与反馈

- [x] 实现全屏点击层、独立在途子弹、枪口/后坐、命中数字、金币飞出和血条。
- [x] 在 75%/50%/25% 生命节点表现装甲破坏。
- [x] 胜负结算只提供返回主菜单，不显示升级或再战。

## T6：WebGL 验证

- [x] 重建场景并运行全部 EditMode：19/19 通过。
- [x] 生成 Gzip WebGL 构建到 `Builds/WebGL`。
- [x] 浏览器检查主菜单、快速点击、存档恢复、失败锁定目标和胜利推进。
- [x] 增加一键本地 HTTP 预览，验证 Gzip、WASM 与数据文件的响应头。
- [x] 更新 README 和实际验证记录。

## 浏览器修复记录

- 首次实测发现战斗点击层未响应；根因是 `FireInput` 与 `GameView` 同文件时没有稳定的场景 MonoScript 引用。
- 已将 `FireInput` 拆成独立脚本，并保留 `0.001` 的不可见点击层 alpha，避免透明网格被射线剔除。
- 修复后浏览器确认：一次点击只消耗一发并造成一次伤害；12 发耗尽后才失败结算。
