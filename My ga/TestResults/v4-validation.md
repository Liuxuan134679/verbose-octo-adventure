# LOOTSHOT v4 验收记录

日期：2026-09-15  
Unity：2022.3.51f1c1

## 自动测试

- EditMode：25 项通过，0 失败。
- 覆盖一击一弹、快速点击排队、弹尽等待、击杀取消尾弹、失败保留金币与目标、首通推进、回刷不推进、战斗中禁止购买、每枪独立强化、金币 +3%、W8/B24 边界、v4 存档往返、备份恢复和完全重置。
- 三条消费路线均通过 B24，无金币死锁。

## WebGL

- Gzip 构建成功，输出目录 `Builds/WebGL`。
- 构建总大小 7.11 MiB，低于 20 MiB 内部目标。
- 本地 HTTP 服务正确返回 `.data.gz`、`.framework.js.gz` 和 `.wasm.gz`。
- 最终新浏览器标签页控制台：0 警告、0 错误。
- 构建后处理启用响应式画布和 `autoSyncPersistentDataPath`。

| 浏览器视口 | 画布 | 页面滚动尺寸 | 结果 |
|---|---|---|---|
| 821×462 | 821×462 | 821×462 | 通过 |
| 907×510 | 907×510 | 907×510 | 通过 |
| 1216×684 | 1216×684 | 1216×684 | 通过 |
| 1280×720 | 1280×720 | 1280×720 | 通过 |

## 浏览器流程

1. 新 v4 存档从 0 金币、W1、B1 开始。
2. 主菜单显示 8 个武器按钮、8×3 Boss 编号面板和 `0/24` 进度。
3. Boss 模型完整位于预览框内，不遮挡名称、收益或选关面板。
4. 进入 B1 后连续点击 6 次，弹量从 12 降到 6，Boss 从 60 降到 0。
5. 结算显示命中金币 +17、击败奖励 +7、首次通关 +20，总金币 44。
6. 结算只显示“返回主菜单”，没有直接再战或战斗中升级。
7. 返回菜单后默认选择 B2，进度为 `1/24`；刷新页面后 44 金币、B2 和进度保持。

## 存档隔离

- Editor/Windows 使用 `lootshot-save-v4-test.json`。
- WebGL 使用 `lootshot-save-v4-test` 和 `lootshot-save-v4-test-backup`。
- 自动测试确认 v4 保存不会改写 v1、v2、v3 文件。
- 重置同时用空白档覆盖 v4 主档和备份；主档随后损坏时只能恢复空白进度，不会复活重置前数据。
